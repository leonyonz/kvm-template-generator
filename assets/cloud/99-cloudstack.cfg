# Enable CloudStack datasource alongside NoCloud/ConfigDrive so the same
# template boots correctly on CloudStack AND on Proxmox (with a cidata ISO).
datasource_list: [ NoCloud, ConfigDrive, CloudStack ]
datasource:
  CloudStack:
    timeout: 120
    max_wait: 120
    retry_interval: 5
